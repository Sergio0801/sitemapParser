{
    "name": "Sitemap Parser",
    "description": "This script checks a sitemap.xml file and retrieves all URLs and generates a list of URLs and h1 and h2 headers on each URL",
    "keywords": ["parser", "xmlParser", "sitemap"],
    "homepage": "/",
    "autoload" : {
        "psr-4": {
            "app\\src\\": "src"
        }
    },
    "require": {
        "php": ">=7.4",
        "vlucas/phpdotenv": "^5.5"
    }
}
